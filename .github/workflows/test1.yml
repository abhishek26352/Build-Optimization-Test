name: Workflow 1
on:
  push:
    branches: [test]

jobs:
  some_job:  
    runs-on: windows-latest
    steps:
      - name: Some Step
        run: echo "Hello! workflow1"

  call-workflow-2:
    uses: ./.github/workflows/test2.yml  # Calls Workflow-2
    secrets: inherit  # Use this if secrets are required

  call-workflow-3:
    uses: ./.github/workflows/test3.yml  # Calls Workflow-3
    secrets: inherit  # Use this if secrets are required
